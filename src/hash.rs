#[derive(Clone, Debug)]
pub enum HashAlgorithm {
    // TODO: Add more algorithms
}

pub struct HashImpl;
impl HashImpl {
    pub fn handle(source: String, raw: Option<bool>, algorithm: HashAlgorithm) {
        // TODO: Implement the logic
    }
}
